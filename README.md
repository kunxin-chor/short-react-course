# short-react-course

To create a new React project, in the terminal type in: `npm create vite@latest`
1. Choose `React`
2. Choose `JavaScript`
3. Just say `yes` to everything else
4. Delete all content from `App.jsx`, `App.css` and `index.css`